version: 2

project_name: inventario

before:
  hooks:
    # Build both frontend bundles. Both are embedded into the binary during the
    # epic #1397 dual-frontend window — INVENTARIO_FRONTEND={legacy|new} picks
    # which one is served at runtime. Once the cutover PR (#1423) lands, the
    # second hook goes away with the legacy bundle.
    - make build-frontend
    - make build-frontend-react

builds:
  - binary: inventario
    main: ./cmd/inventario
    dir: go
    flags:
      # Use build tags to include frontend assets
      - -tags=with_frontend
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/denisvmedia/inventario/internal/version.Version={{.Version}}
      - -X github.com/denisvmedia/inventario/internal/version.Commit={{.Commit}}
      - -X github.com/denisvmedia/inventario/internal/version.Date={{.Date}}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - formats: [ 'tar.gz' ]
    wrap_in_directory: true
    format_overrides:
      - goos: windows
        formats: [ 'zip' ]
    name_template: '{{ .ProjectName }}-{{ .Version }}-{{ .Os }}-{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}'
    files:
      - LICENSE
      - README.md

# Docker images are built by .github/workflows/docker.yml (matrix build on
# native amd64/arm64 runners + buildx imagetools merge). Goreleaser keeps
# responsibility for binaries, archives, checksums and the GitHub Release.

snapshot:
  version_template: SNAPSHOT-{{ .Commit }}

checksum:
  disable: false
  name_template: '{{ .ProjectName }}-{{ .Version }}-checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^build:'
      - Merge pull request
      - Merge branch
      - go mod tidy

release:
  skip_upload: false
  github:
    owner: denisvmedia
    name: inventario
  header: |
    ## Inventario v{{ .Version }}
    
    This release includes the following changes:
  footer: |
    
    ## Installation
    
    ### Using pre-built binaries
    
    Download the appropriate binary for your platform from the assets below.
    
    ### Using Go
    
    ```bash
    go install github.com/denisvmedia/inventario@v{{ .Version }}
    ```

source:
  enabled: true
  name_template: '{{ .ProjectName }}-{{ .Version }}-source'
