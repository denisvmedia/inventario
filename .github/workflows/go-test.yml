name: Go Test

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: read
  pull-requests: read

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.1'
          cache: true
      - name: Install dependencies
        run: go mod download
      - name: Create dist directory
        run: mkdir -p frontend/dist && echo "" > frontend/dist/.gitkeep
      - name: Run tests
        run: go test -v -race ./...
