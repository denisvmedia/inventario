# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates
#
# NOTE: `design-mocks/` is intentionally NOT listed below. It is a read-only
# mirror of github.com/denisvmedia/inventario-design synchronized by an external
# tool (see AGENTS.md "Design contract & frontend workflow"). Dependency bumps
# on its package.json would be wiped on the next sync, so they must not happen
# here. Renovate is configured to skip the same path via `ignorePaths` in
# renovate.json. Do not add a `design-mocks/` directory entry here.

version: 2
updates:
  - package-ecosystem: "gomod" # See documentation for possible values
    directory: "/go" # Location of package manifests
    schedule:
      interval: "weekly"
    allow:
      - dependency-type: "direct"
      - dependency-type: "indirect"
    groups:
      cloud-sdks:
        patterns:
          - "cloud.google.com/*"
          - "github.com/Azure/azure-sdk-for-go/*"
          - "github.com/Azure/go-autorest/*"
          - "github.com/aws/aws-sdk-go"
          - "github.com/aws/aws-sdk-go-v2"
          - "github.com/aws/aws-sdk-go-v2/*"
          - "github.com/digitalocean/*"
          - "github.com/minio/minio-go/*"
      golang:
        patterns:
          - "golang.org/x/*"
          - "google.golang.org/*"
      openapi:
        patterns:
          - "github.com/go-openapi/*"

  - package-ecosystem: "npm" # See documentation for possible values
    directory: "/frontend" # Location of package manifests
    schedule:
      interval: "weekly"
    groups:
      vitest:
        patterns:
          - "vitest"
          - "@vitest/*"

  - package-ecosystem: "npm" # See documentation for possible values
    directory: "/e2e" # Location of package manifests
    schedule:
      interval: "weekly"
    groups:
      vitest:
        patterns:
          - "vitest"
          - "@vitest/*"
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: "weekly"

  # Docker
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "UTC"
    open-pull-requests-limit: 5
    reviewers:
      - "denisvmedia"
    assignees:
      - "denisvmedia"
    commit-message:
      prefix: "docker"
      include: "scope"
    labels:
      - "dependencies"
      - "docker"

  # Docker Example
  - package-ecosystem: "docker-compose"
    directory: "/example"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "UTC"
    open-pull-requests-limit: 5
    reviewers:
      - "denisvmedia"
    assignees:
      - "denisvmedia"
    commit-message:
      prefix: "docker"
      include: "scope"
    labels:
      - "dependencies"
      - "docker"
