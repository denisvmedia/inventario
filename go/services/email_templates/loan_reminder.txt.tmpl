Hi {{.Name}},

{{if .LoanIsOverdue -}}
{{if .CommodityName}}{{.CommodityName}}{{else}}Your item{{end}} is OVERDUE — it was due back on {{.DueBackAt}}{{if gt .LoanDaysDelta 0}} ({{.LoanDaysDelta}} day{{if ne .LoanDaysDelta 1}}s{{end}} ago){{end}}.
{{else if .LoanIsDueSoon -}}
{{if .CommodityName}}{{.CommodityName}}{{else}}Your item{{end}} is due back on {{.DueBackAt}}{{if gt .LoanDaysDelta 0}} (in {{.LoanDaysDelta}} day{{if ne .LoanDaysDelta 1}}s{{end}}){{end}}.
{{else -}}
You have a loan reminder for {{if .CommodityName}}{{.CommodityName}}{{else}}your item{{end}}.
{{end}}
Borrowed by {{.BorrowerName}} on {{.LentAt}}.
{{if .CommodityURL}}
Open the item to nudge the borrower or log a return:
{{.CommodityURL}}
{{end}}
Inventario sends this once per loan so you don't lose track of items
you've lent out.
