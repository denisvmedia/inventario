apiVersion: batch/v1
kind: Job
metadata:
  name: minio-create-bucket
  namespace: inventario-dev
  labels:
    app.kubernetes.io/name: minio-create-bucket
    app.kubernetes.io/component: data-service
    app.kubernetes.io/part-of: inventario
    app.kubernetes.io/managed-by: raw-yaml
    inventario.io/environment: dev
spec:
  backoffLimit: 6
  template:
    metadata:
      labels:
        app.kubernetes.io/name: minio-create-bucket
        app.kubernetes.io/component: data-service
        app.kubernetes.io/part-of: inventario
        app.kubernetes.io/managed-by: raw-yaml
        inventario.io/environment: dev
    spec:
      restartPolicy: OnFailure
      containers:
        # The same image as the server: it ships the client as /usr/bin/mcli
        # with an `mc` symlink, so the commands below are unchanged.
        - name: mc
          image: pgsty/silo:RELEASE.2026-09-16T00-00-00Z
          imagePullPolicy: IfNotPresent
          env:
            - name: MC_CONFIG_DIR
              value: /tmp/.mc
          envFrom:
            - secretRef:
                name: minio-credentials
          volumeMounts:
            - name: mc-config
              mountPath: /tmp/.mc
          command:
            - /bin/sh
            - -c
            - |
              set -e
              until mc alias set dev http://minio:9000 "$MINIO_ROOT_USER" "$MINIO_ROOT_PASSWORD"; do
                echo "Waiting for the object store to become ready..."
                sleep 2
              done
              mc mb --ignore-existing dev/"$MINIO_BUCKET_NAME"
      volumes:
        - name: mc-config
          emptyDir: {}
